# Centralized Chassis — High-Level Design

This directory is the **placeholder location** for all **High-Level Design (HLD)** documents for the **Centralized Chassis** project.

Add new HLDs here as separate Markdown files (or subdirectories, if a topic spans multiple documents) so Cisco-specific chassis design material stays grouped and easy to discover alongside other `doc/cisco/` content.
